import { Balance, NftCollection, NftUtxo, Provider, Token, TokenBalance, TokenUtxo, Utxo } from "@sensible-contract/abstract-provider";
export declare type NETWORK = "mainnet" | "testnet";
export declare type PROVIDER_NAME = "sensiblequery" | "metasv" | "whatsonchain";
export declare class BaseProvider implements Provider {
    network: "mainnet" | "testnet";
    name: PROVIDER_NAME;
    constructor(network: NETWORK, name: PROVIDER_NAME);
    getRawTx(txid: string): Promise<string>;
    broadcast(rawtx: string): Promise<string>;
    getIsUtxoSpent(txId: string, outputIndex: number): Promise<boolean>;
    getUtxos(address: string, queryParams?: {
        cursor: number;
        size: number;
    }): Promise<Utxo[]>;
    getBalance(address: string): Promise<Balance>;
    getTokenUtxos(codehash: string, genesis: string, address: string, queryParams?: {
        cursor: number;
        size: number;
    }): Promise<TokenUtxo[]>;
    getTokenBalance(codehash: string, genesis: string, address: string): Promise<TokenBalance>;
    getTokenList(address: string): Promise<Token[]>;
    getNftUtxos(codehash: string, genesis: string, address: string, queryParams?: {
        cursor: number;
        size: number;
    }): Promise<NftUtxo[]>;
    getNftUtxo(codehash: string, genesis: string, tokenIndex: string): Promise<NftUtxo>;
    getNftCollectionList(address: string, queryParams?: {
        cursor: number;
        size: number;
    }): Promise<NftCollection[]>;
}
