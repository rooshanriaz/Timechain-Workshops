import { Balance, Utxo } from "@sensible-contract/abstract-provider";
import { BaseProvider, NETWORK } from "./base-provider";
export declare class WhatsOnChainProvider extends BaseProvider {
    apiPrefix: string;
    constructor(network?: NETWORK);
    getUtxos(address: string, queryParams?: {
        cursor: number;
        size: number;
    }): Promise<Utxo[]>;
    getRawTx(txid: string): Promise<string>;
    broadcast(rawtx: string): Promise<string>;
    getBalance(address: string): Promise<Balance>;
}
