import { API_NET } from "sensible-sdk";
import { AuthorizationOption } from "sensible-sdk/dist/sensible-api";
export default class Sensible {
    network: API_NET;
    feeb: number;
    tokenWif: string;
    metasvAuth: AuthorizationOption;
    constructor(network: API_NET, feeb: number, tokenWif: string, metasvAuth: AuthorizationOption);
    ftMergeTime(codehash: string, genesis: string, sensibleId: string): Promise<number>;
    ftMergeOne(codehash: string, genesis: string, sensibleId: string, purseWif: string): Promise<boolean>;
    ftMerge(codehash: string, genesis: string, sensibleId: string, purseWif: string): Promise<void>;
}
